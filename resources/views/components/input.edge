<input
  class="form-control mb-3"
  id="{{ name }}"
  name="{{ name }}"
  type="{{ type ? type : 'text' }}"
  @if(placeholder)
    placeholder="{{ placeholder }}"
  @endif
  @if(value)
    value="{{ value }}"
  @endif
  {{ required ? 'required' : null }}
>

